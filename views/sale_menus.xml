<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <record id="action_open_stock_quant" model="ir.actions.act_window">
        <field name="name">Ubicaciones</field>
        <field name="res_model">stock.quant</field>
        <field name="view_mode">list</field>
        <field name="view_id" ref="stock.view_stock_quant_tree_editable"/>
        <field name="target">current</field>
        <!-- <field name="context">{'search_default_product': true}</field> -->
    </record>

    <!-- Luego el menú que usa la acción -->
    <menuitem id="menu_sale_stock_quant" 
        name="Ubicaciones" 
        action="action_open_stock_quant" 
        sequence="35" 
        parent="sale.sale_menu_root"
        />
    
</odoo>